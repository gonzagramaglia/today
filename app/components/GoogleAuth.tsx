'use client'

import { Language } from '../data/i18n'

interface GoogleAuthProps {
    lang: Language;
    variant?: 'icon' | 'full';
}

export default function GoogleAuth(_props: GoogleAuthProps) {
    return null;
}
